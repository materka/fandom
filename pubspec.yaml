name: fandom
description: Fandom API library, which makes it easy to access data from specified fandom URL
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  json_serializable: ^3.3.0
  json_annotation: ^3.0.1

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
  http: ^0.12.2
  dio: ^3.0.9
  build_runner: ^1.0.0
